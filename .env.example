# Environment Variables for JadaaMart E-commerce Platform

# Node Environment
NODE_ENV=production

# Database Configuration (MySQL/MariaDB)
# For local development (uncomment and configure):
# DB_HOST=127.0.0.1
# DB_PORT=3307
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=jadaamart_db

# For Production (Vercel with Cloud Database)
# Example with Vercel Postgres or MySQL Cloud Service:
DB_HOST=your-database-host.com
DB_PORT=3306
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password
DB_NAME=jadaamart_db

# JWT Secret (Change this to a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Port (Vercel will override this automatically)
PORT=8080

# ========================================
# IMPORTANT NOTES:
# ========================================
# 1. Copy this file to .env (without .example extension)
# 2. Never commit .env to Git (it's in .gitignore)
# 3. In Vercel Dashboard:
#    - Go to Project Settings > Environment Variables
#    - Add all production variables there
# 4. For local development, use local database settings
# 5. Change JWT_SECRET to a secure random string
